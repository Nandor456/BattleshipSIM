import javax.swing.*;

public class FightPanel extends JFrame {
    public FightPanel() {
        setTitle("Battleship SIM");
        setDefaultCloseOperation(EXIT_ON_CLOSE);
        setSize(600, 600);
    }
}
